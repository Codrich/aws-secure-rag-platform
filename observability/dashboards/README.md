# Observability (Phase 7)

CloudWatch dashboards and alarms as code: API requests/errors, p50/p95
latency, Bedrock invocation failures, input/output tokens, estimated cost,
retrieved-document count, guardrail interventions, prompt-attack detections,
PII detections, empty retrievals, ingestion failures/DLQ depth.
