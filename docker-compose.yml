services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      - APP_ENV=dev
    volumes:
      - ./app:/srv/app:ro
    command: uvicorn app.api.main:app --host 0.0.0.0 --port 8000 --reload
