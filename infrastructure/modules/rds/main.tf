# RDS PostgreSQL + pgvector module (Phase 2).
# Planned resources: encrypted (KMS) db instance in private subnets,
# no public access, IAM auth, automated backups, parameter group
# enabling pgvector, security group allowing ingress from ECS tasks only.
