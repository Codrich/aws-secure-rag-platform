# Policy-as-code (Phase 6)

OPA/Conftest policies enforced in CI against Terraform plans:
no public IPs on ECS tasks, encryption required on all data stores,
no wildcard IAM actions, mandatory tags, approved regions only.
